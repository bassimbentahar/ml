import React from  'react'
import { Paper} from "@material-ui/core";

const leftPanel=({styles})=>{
    return(
        <Paper style={styles.Paper}>
            Left Panel
        </Paper>
    )
}

export default leftPanel;
